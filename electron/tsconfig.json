{
  "// note": "Type CHECKING only, same as the server: Electron runs these .ts files directly via Node type stripping, so all syntax must be erasable. Kept separate from the root config because the shell is not part of the server's compilation.",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["**/*.ts"]
}
