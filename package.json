{
  "name": "crokinole-app",
  "version": "2.0.0",
  "private": true,
  "description": "Crokinole tournament scoring: phone entry, live big board, knockout bracket, and NCA Field-Weighted Points season ranking",
  "type": "module",
  "scripts": {
    "start": "node src/server.ts",
    "dev": "node --watch src/server.ts",
    "test": "node --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit",
    "typecheck:app": "tsc -p electron --noEmit",
    "app": "electron electron/main.ts",
    "dist": "electron-builder --mac dmg --arm64 && electron-builder --mac dmg --x64",
    "lint": "eslint .",
    "check": "npm run lint && npm run typecheck && npm test"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "express": "^5.1.0",
    "electron-updater": "^6.8.9"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "@types/express": "^5.0.6",
    "@types/node": "^24.7.0",
    "electron": "^44.2.0",
    "electron-builder": "^26.15.3",
    "eslint": "^9.36.0",
    "globals": "^15.15.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.45.0"
  }
}
